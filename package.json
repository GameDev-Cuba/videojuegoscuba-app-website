{
  "name": "videojuegoscuba-app-data",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node src/build.js",
    "start": "npx nodemon --exec ./build.sh --watch src --watch content -e html,json,md,css,png,jpg",
    "server": "npx http-server website",
    "optipng": "optipng content/**/assets/*"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^20.6.0",
    "parse-md": "^3.0.3"
  }
}
